let userNumber = Number(
  prompt("Escreva um número positivo e inteiro para fazer a contagem: ")
);

for (let i = 0; i <= userNumber; i++) {
  console.log(i);
}

//contagem de 0 ate o número escolhido
